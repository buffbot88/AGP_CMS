@page "/cms/admin/downloads"
@model LegendaryCMS.Pages.Admin.DownloadsModel
@{
    ViewData["Title"] = "Download Management";
}

<style>
    .admin-container {
        max-width: 1400px;
        margin: 20px auto;
        padding: 20px;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    }
    
    .page-header {
        background: linear-gradient(135deg, #06b6d4 0%, #0891b2 100%);
        color: white;
        padding: 30px;
        border-radius: 10px;
        margin-bottom: 30px;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }
    
    .breadcrumb {
        margin-bottom: 20px;
        color: #666;
    }
    
    .breadcrumb a {
        color: #667eea;
        text-decoration: none;
    }
    
    .panel {
        background: white;
        padding: 25px;
        border-radius: 10px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        margin-bottom: 20px;
    }
    
    .panel h2 {
        margin: 0 0 20px 0;
        color: #333;
        border-bottom: 2px solid #06b6d4;
        padding-bottom: 10px;
    }
    
    .btn {
        padding: 10px 20px;
        border-radius: 6px;
        border: none;
        cursor: pointer;
        font-weight: 600;
        transition: all 0.3s;
        text-decoration: none;
        display: inline-block;
    }
    
    .btn-primary {
        background: #06b6d4;
        color: white;
    }
    
    .btn-primary:hover {
        background: #0891b2;
    }
    
    .btn-danger {
        background: #ef4444;
        color: white;
        font-size: 0.85em;
        padding: 8px 16px;
    }
    
    .alert {
        padding: 15px;
        border-radius: 6px;
        margin-bottom: 20px;
    }
    
    .alert-success {
        background: #d1fae5;
        color: #065f46;
        border: 1px solid #a7f3d0;
    }
    
    .alert-error {
        background: #fee2e2;
        color: #991b1b;
        border: 1px solid #fecaca;
    }
    
    .downloads-table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
    }
    
    .downloads-table th {
        background: #f9fafb;
        padding: 12px;
        text-align: left;
        border-bottom: 2px solid #e5e7eb;
        font-weight: 600;
        color: #333;
    }
    
    .downloads-table td {
        padding: 12px;
        border-bottom: 1px solid #e5e7eb;
    }
    
    .empty-state {
        text-align: center;
        padding: 60px 20px;
        color: #999;
    }
    
    .empty-state .icon {
        font-size: 4em;
        margin-bottom: 20px;
    }
</style>

<div class="admin-container">
    <div class="breadcrumb">
        <a href="/cms/admin">Admin</a> / Download Management
    </div>
    
    <div class="page-header">
        <h1>📥 Download Management</h1>
        <p>Manage downloadable files and resources</p>
    </div>
    
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">
            ✓ @TempData["Success"]
        </div>
    }
    
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-error">
            ✗ @TempData["Error"]
        </div>
    }
    
    <div class="panel">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
            <h2 style="margin: 0;">Downloads</h2>
            <a href="/cms/admin/downloads/upload" class="btn btn-primary">Upload New File</a>
        </div>
        
        @if (Model.Downloads.Any())
        {
            <table class="downloads-table">
                <thead>
                    <tr>
                        <th>File Name</th>
                        <th>Description</th>
                        <th>Size</th>
                        <th>Downloads</th>
                        <th>Uploaded</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var download in Model.Downloads)
                    {
                        <tr>
                            <td><strong>@download.FileName</strong></td>
                            <td>@download.Description</td>
                            <td>@download.FileSize</td>
                            <td>@download.DownloadCount</td>
                            <td>@download.UploadedAt.ToString("MMM dd, yyyy")</td>
                            <td>
                                <form method="post" asp-page-handler="Delete" style="display: inline;">
                                    <input type="hidden" name="downloadId" value="@download.Id" />
                                    <button type="submit" class="btn btn-danger" 
                                            onclick="return confirm('Are you sure you want to delete this file?');">
                                        Delete
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="empty-state">
                <div class="icon">📥</div>
                <p><strong>No downloads yet</strong></p>
                <p>Upload files to make them available for download</p>
            </div>
        }
    </div>
</div>
